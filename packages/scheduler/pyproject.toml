[project]
    name            = "dataforge-scheduler"
    version         = "1.0.0"
    description     = "Data-Forge scheduler runtime package"
    requires-python = ">=3.14"
    dependencies    = [
    "grpcio>=1.81.0",
    "protobuf>=7.35.0",
]

[build-system]
    requires      = ["hatchling"]
    build-backend = "hatchling.build"

[tool.ruff]
    target-version = "py314"
    line-length    = 160
    indent-width   = 4
    extend-exclude = ["dataforge_protocol"]

[tool.mypy]
    mypy_path = [
        ".",
        "../..",
    ]
    exclude = ["(^|/)conftest\\.py$", "dataforge_protocol"]

[tool.pytest.ini_options]
    cache_dir = "tests/.artifacts/pytest-cache"

[dependency-groups]
    dev = [
        "mypy>=2.1.0",
        "pytest>=9.0.3",
        "pytest-asyncio>=1.4.0",
        "pytest-cov>=7.1.0",
        "pytest-timeout>=2.4.0",
        "pytest-xdist>=3.8.0",
        "ruff>=0.15.17",
        "types-grpcio>=1.0.0.20260607",
]

[tool.hatch.build.targets.wheel]
    only-include = ["main.py", "dataforge_protocol", "scheduler_grpc"]
