[project]
    name            = "dataforge-scheduler"
    version         = "1.0.0"
    description     = "Data-Forge scheduler runtime package"
    requires-python = ">=3.14"
    dependencies    = [
    "grpcio>=1.83.0",
    "protobuf>=7.36.0",
    "protovalidate>=2.0.0",
    "psycopg[binary]>=3.3.4",
]

[build-system]
    requires      = ["hatchling"]
    build-backend = "hatchling.build"

[tool.ruff]
    target-version = "py314"
    line-length    = 160
    indent-width   = 4
    extend-exclude = ["buf", "dataforge_protocol"]

[tool.mypy]
    mypy_path = [
        ".",
        "../..",
    ]
    exclude = ["(^|/)conftest\\.py$", "buf", "dataforge_protocol"]

[tool.pytest.ini_options]
    cache_dir = "tests/.artifacts/pytest-cache"

[dependency-groups]
    dev = [
        "mypy>=2.3.1",
        "pytest>=9.1.1",
        "pytest-asyncio>=1.4.0",
        "pytest-cov>=7.1.0",
        "pytest-timeout>=2.4.0",
        "pytest-xdist>=3.8.0",
        "ruff>=0.15.17",
        "types-grpcio>=1.83.0.20260730",
]

[tool.hatch.build.targets.wheel]
    only-include = ["main.py", "buf", "dataforge_protocol", "scheduler_grpc"]
