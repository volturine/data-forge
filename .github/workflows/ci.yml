name: CI

on:
  pull_request:
    branches: [dev, master]
  push:
    branches: [dev, master]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    runs-on: [self-hosted, linux, x64]
    steps:
      - uses: actions/checkout@v6

      - uses: astral-sh/setup-uv@v8.1.0
        with:
          version: "0.10.4"

      - uses: actions/setup-python@v6
        with:
          python-version: "3.14.2"

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.11"

      - uses: actions/setup-node@v6
        with:
          node-version: "24"

      - uses: taiki-e/install-action@v2
        with:
          tool: just@1.51.0

      - name: Generate protocol code
        run: just generate-protocol

      - name: Install backend dependencies
        working-directory: packages/backend
        run: uv sync

      - name: Install scheduler dependencies
        working-directory: packages/scheduler
        run: uv sync

      - name: Install worker dependencies
        working-directory: packages/worker
        run: uv sync

      - name: Install frontend dependencies
        working-directory: packages/frontend
        run: bun install --frozen-lockfile

      - name: Canonical verification
        run: just verify

      - name: Canonical tests
        run: |
          # Self-hosted runners retain resources from prior jobs. The engine
          # integration tests build an image, so reclaim all unused Docker
          # state instead of retaining a day's build cache.
          docker container prune --force
          docker volume prune --force
          docker builder prune --all --force
          docker image prune --force
          docker system df
          df -h
          just test

  frontend:
    name: frontend
    runs-on: [self-hosted, linux, x64]
    steps:
      - uses: actions/checkout@v6

      - uses: astral-sh/setup-uv@v8.1.0
        with:
          version: "0.10.4"

      - uses: actions/setup-python@v6
        with:
          python-version: "3.14.2"

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.11"

      - uses: actions/setup-node@v6
        with:
          node-version: "24"

      - uses: taiki-e/install-action@v2
        with:
          tool: just@1.51.0

      - name: Generate protocol code
        run: just generate-protocol

      - name: Install frontend dependencies
        working-directory: packages/frontend
        run: bun install --frozen-lockfile

      - name: Frontend static checks
        working-directory: packages/frontend
        run: bun run panda:codegen && bun run check && bun run lint

      - name: Frontend unit tests
        working-directory: packages/frontend
        run: bun run test:unit

  e2e:
    name: e2e
    runs-on: [self-hosted, linux, x64]
    if: github.event_name == 'pull_request' && github.base_ref == 'master'
    timeout-minutes: 50
    permissions:
      contents: read
    env:
      PORT: "8001"
      FRONTEND_PORT: "3001"
      # Native Docker engines add multi-second cold starts. Parallel Playwright
      # workers share one default namespace and race on create/delete, which
      # shows up as empty galleries while waiting for the analysis editor.
      # Serialize browsers on CI; keep enough engine slots for sequential work.
      PW_E2E_WORKERS: "1"
      BUILD_WORKER_MIN_PROCESSES: "1"
      BUILD_WORKER_MAX_PROCESSES: "1"
      COMPUTE_REQUEST_CONCURRENCY: "2"
      MAX_CONCURRENT_ENGINES: "6"
      POLARS_MAX_MEMORY_MB: "384"
      E2E_ENGINE_IDLE_TTL_SECONDS: "20"
      E2E_ENGINE_IDLE_REAP_INTERVAL_SECONDS: "5"
      # Serial browser + up to two retries when mostly green (~30m observed).
      E2E_TIMEOUT_SECONDS: "2700"
      # Keep service logs quiet enough that logging I/O does not contend with
      # page loads; capacity issues still surface via failed compute requests.
      LOG_LEVEL: "warning"
    steps:
      - uses: actions/checkout@v6

      - uses: astral-sh/setup-uv@v8.1.0
        with:
          enable-cache: false
          version: "0.10.4"

      - uses: actions/setup-python@v6
        with:
          python-version: "3.14.2"

      - uses: actions/setup-node@v6
        with:
          node-version: "24"

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.11"

      - uses: taiki-e/install-action@v2
        with:
          tool: just@1.51.0

      - name: Generate protocol code
        run: just generate-protocol

      - name: Install backend dependencies
        working-directory: packages/backend
        run: uv sync

      - name: Install frontend dependencies
        working-directory: packages/frontend
        run: bun install --frozen-lockfile

      - name: Install Playwright browsers
        working-directory: packages/frontend
        run: bun x playwright install --with-deps chromium

      - name: Reclaim unused Docker state
        run: |
          # E2E provisions the native engine image. This runner is shared
          # across jobs, so stale stopped containers, unused volumes, and
          # build cache must not consume the image build's disk budget.
          docker container prune --force
          docker volume prune --force
          docker builder prune --all --force
          docker image prune --force
          docker system df
          df -h

      - name: Run e2e tests
        run: |
          set -o pipefail
          mkdir -p .artifacts/ci/e2e
          export E2E_LOG_DIR="$PWD/.artifacts/ci/e2e/services"
          export DATAFORGE_SKIP_PROTOCOL_GENERATE=1
          just test-e2e 2>&1 | tee .artifacts/ci/e2e/e2e.log
          status=${PIPESTATUS[0]}
          if [ "$status" -eq 124 ]; then
            echo "e2e step hit the harness timeout (E2E_TIMEOUT_SECONDS) before the job timeout"
          fi
          exit "$status"

      - name: Upload e2e diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-diagnostics-${{ github.run_id }}
          if-no-files-found: warn
          include-hidden-files: true
          path: |
            .artifacts/ci/e2e/
            packages/frontend/tests/.artifacts/playwright/

  publish-images:
    if: github.event_name == 'push' && (github.ref_name == 'dev' || github.ref_name == 'master')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - image_name: data-forge-api
            target: api
          - image_name: data-forge-scheduler
            target: scheduler
          - image_name: data-forge-worker
            target: worker
          - image_name: data-forge-polars-engine
            target: engine
    steps:
      - uses: actions/checkout@v6

      - uses: docker/setup-buildx-action@v4

      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Derive image tags
        id: tags
        run: |
          short_sha="${GITHUB_SHA::12}"
          branch_tag="${GITHUB_REF_NAME}"
          {
            echo 'tags<<EOF'
            echo "ghcr.io/${GITHUB_REPOSITORY_OWNER}/${{ matrix.image_name }}:${branch_tag}"
            echo "ghcr.io/${GITHUB_REPOSITORY_OWNER}/${{ matrix.image_name }}:sha-${short_sha}"
            if [ "$GITHUB_REF_NAME" = "master" ]; then
              echo "ghcr.io/${GITHUB_REPOSITORY_OWNER}/${{ matrix.image_name }}:latest"
            fi
            echo 'EOF'
          } >> "$GITHUB_OUTPUT"

      - name: Build and push ${{ matrix.image_name }}
        uses: docker/build-push-action@v7
        with:
          context: .
          file: docker/Dockerfile
          target: ${{ matrix.target }}
          push: true
          tags: ${{ steps.tags.outputs.tags }}
